# Week 13：2026.06.08 - 2026.06.14

## 本周完成

- [x] 继续修改 Overleaf 新稿 `main.tex`
- [x] 将 Problem Statement 改成 certified formation planning 表述
- [x] 新增 workspace / inflated obstacle / obstacle-free space / certified convex region 定义
- [x] 新增 formation template、placement、outer vertices 和 envelope 定义
- [x] 将 placement 写成自由姿态形式 `z=(t,s,R)`
- [x] 将 LTL labeling 改成由 certified placement 诱导的 `L(z,f,e)`
- [x] 将总问题改为 `Certified Navigation with Automated Formation`
- [x] 开始重写 Section III-A / III-B，使 DFTS 从普通 waypoint abstraction 变成 certified abstraction
- [x] 阅读并对比新论文 `Bellman Value Decomposition for Task Logic in Safe Optimal Control`

## 主要进展

本周主要做论文文字和数学定义补充，没有重点推进新的 demo。

Problem Statement 已经从原 CDC 的 grid waypoint 版本推进到：

```text
workspace
-> inflated obstacles
-> obstacle-free space F_bar
-> certified convex regions P_i subset F_bar
-> formation placement z=(t,s,R)
-> envelope E(z,f)
-> certified feasible placement
```

核心安全表达变成：

```text
E(z,f) subset P_i subset F_bar
```

因为 `P_i` 是 convex polytope，实际检查可以转成 outer vertices containment：

```text
V(z,f) subset P_i
```

## 关键认识

当前论文不应继续写成普通 waypoint abstraction。更准确的叙事是：

```text
state = certified region + formation template
```

并且每个 symbolic state 都要关联一个 certified placement：

```text
z_i_f = formation(P_i,f)
```

这样 symbolic layer 选择的不是抽象点，而是一个被凸区域认证过的 formation placement。

Section III-A / III-B 的修改方向也已明确：

```text
certified states
-> Allowed(P)
-> bridge states from intersections
-> certified edges with convex certificates
-> GR(1) strategy synthesis
```

## 遇到的问题

- `Allowed(P)`、`formation(P,f)`、bridge state 和 edge certificate 的符号还需要继续统一
- Section III 中旧的 `W x F` waypoint 写法还要逐步替换
- LaTeX 中部分长公式和 history signal 容易造成 underfull warning
- Case Study 仍是旧 CDC grid / battery 示例，还没有开始替换
- continuous layer 还没有完全和新的 certified abstraction 对齐

## 下周计划

- 继续整理 Section III-A / III-B 的最终写法
- 把 certified DFTS、bridge state、edge certificate 写得更紧凑
- 检查 `main.tex` 中重复 label、旧符号和旧 CDC 叙事残留
- 再决定 Case Study 如何从旧 grid 示例换成新的搬运任务
- 保持理论叙事和后续 demo 一致：`z=(t,s,R)`、free orientation、certified placement
